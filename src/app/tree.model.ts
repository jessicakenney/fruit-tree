export class Tree {
  constructor (public type: string, public species: string, public street: string, public zip: string, public notes: string, public image: string, public ownerUsername: string) { }
}
