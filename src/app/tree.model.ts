export class Tree {
  public numLikes : number = 0;
  constructor (public type: string, public species: string, public street: string, public zip: string, public notes: string, public image: string, public userId: string) { }
}
